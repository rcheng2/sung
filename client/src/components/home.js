import Logout from './Logout';
export default function Home() {
    return (
        <main style={{ padding: "1rem 0" }}>
            <h2>Home Page</h2>
            <Logout />
        </main>
    );
}